//WAP to count number of words in a sentence.

#include <stdio.h>
#include <conio.h>
#include <string.h>

int main()
{
    char st[100];
    int i , c=0 ;

    printf("\n");

    printf("Enter a sentence : ");
    gets(st);

   for(i=0; s[i]!='\0'; i++)
   {
      printf("%s", s[i]);

      if(s[i]==' ')
      {
         printf("\n");
      }
   }
    
    getch();
    return 0;
}
